rules:
  - id: spring-csrf-disabled
    languages: [java]
    message: "Внимание! Обнаружено отключение защиты CSRF (.csrf().disable()). Убедитесь, что это безопасно для вашего типа авторизации."
    severity: WARNING
    pattern: $ANY.csrf().disable()