rules:
  - id: java-use-of-md5
    languages: [java]
    message: "Внимание! Обнаружено использование небезопасного алгоритма хэширования MD5. Рекомендуется использовать SHA-256 или bcrypt."
    severity: ERROR
    pattern: MessageDigest.getInstance("MD5")