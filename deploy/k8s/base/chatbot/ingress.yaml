apiVersion: v1
kind: Service
metadata:
  name: crapi-chatbot-mcp
  labels:
    app: crapi-chatbot
spec:
  ports:
  - name: mcp
    port: 5500
    nodePort: 30500
    protocol: TCP
  selector:
    app: crapi-chatbot
  sessionAffinity: None
  type: LoadBalancer
